he is talking about bootstrapping

some other examples

incindent \* 0.08 P
detector that guess 90% of critical and 12% of non critilcal
P(critical|flagged) = ?
$$
P(critical|flagged ) =
\frac{P(flagged|critical)P(critical)}{P(flagged)} = 
\frac{(0.9)*0.08}{0.08*0.9 + (1-0.08)*0.12} =
0.395
$$
idk if its right 

prof method
$$
\begin{aligned}
	P(Critica) = 0.08 \\
	P(Non\ Critical) = 0.92 \\
	P(F | Non\ Critical ) = 0.12 \\
	P(F | Critical ) = 0.9 \\
	P(critical|flagged ) = \frac{P(flagged|critical)P(critical)}{P(flagged)} \\ \\
	\text{we miss P(F) what we can do?}\\
	P(Non\ critical|flagged ) = \ - P(critical|flagged )\\
	P(Non\ critical|flagged ) = \frac{P(flagged|\ critical)P(\ critical)}{P(flagged)}\\ \\
	P(flagged) = P(critical|flagged ) + P(Non\ critical|flagged) \\
	\text{i think i make this right :)}
\end{aligned}
$$


## Es Q3
$$
\begin{aligned}
\text{R mean = 200 ms} \\
\text{R variance = 2500 ms} \\
\text{G mean = 800 ms} \\
\text{G variance = 1000 ms} \\
\text{R and G are IID} \\ \\
mean\ T = R + G = ?\\
variance\ T = R + G = ?\\ \\
\text{the expectation are linear} \\
\text{its just a sum :)}\\
E[T] = E[R] + E[G] = 200 + 800 = 1000
Var(T) = Var(R) + Var(G) = 2500 + 10000 = 12500
\end{aligned}
$$


## Es Q4
# ==U need to recognize by yourself the CLT==



## tips and tricks 
beta distribution over point estimate

if we have more test we are more shure and the beta will be more pointy

distribution are more concetrate if they are extreme, 

a beta of 5,5 is less concetrate maybe

if we test 10 different model variance on the same test the model will be baiased

if we have question we can send email



