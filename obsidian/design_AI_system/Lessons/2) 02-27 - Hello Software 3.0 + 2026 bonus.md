introduction to llm 

tiktokenizer
- before choose a text and then chose how to take tokens
different models have different token

we want to unique id stuff

idea is make it compact 
so we assign tokens to the most frequent combination of symbols 

mapping into symbols in to token spaces


### Step 3 neural network training 
u ask the model given some number, which number should be the right number?
probability for every token 

### Step 4 inference 
- to generate data, just predict one token at time 

at the start there are just numbers and the model is just mapping 

llm is functions bc there are many parameters, 
u take this massive text, transform it in tokens
and expect tokens in exchange 

base mode --> model that is not gone to fine tuning and other types of training

fine tuning is very difficult, and is also very expensive, new science that is emerging 

yt talk (1y old :( ) linked in the web pages 


---
he opened vscode

# Demo Notebook: Hello software 3.0
preview of next week lesson