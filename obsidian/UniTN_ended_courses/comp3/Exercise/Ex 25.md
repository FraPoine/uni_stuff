Consider the following classical NP-complete languages: 
- CLIQUE = {(G, k) : Undirected graph G has a completely connected subgraph of size k}, 
- INDSET = {(G, k) : Undirected graph G has a completely disconnected subgraph of size k}. 
1) Describe a polynomial-time reduction from one language to the other. 
2) Show that CLIQUE ∩ INDSET ≠ ∅. 
*Hint — For 1, choose the direction you like. In 2, don’t be afraid of simple answers: to show that a set is not empty, you just need to find an element in it.*

---
### CLIQUE
- {(G, k) : Undirected graph G has a completely connected subgraph of size k}

### INDSET 
- {(G, k) : Undirected graph G has a completely disconnected subgraph of size k}. 

## 1
**Describe a polynomial-time reduction from one language to the other.**

As usual lets start with 
- Graph G = (V, E)
- integer k
are part of both problem

Graph G has a CLIQUE of size k iff graph !G has an INDSET of size k

C’è una corrispondenza diretta:

- Un **clique** di dimensione k in G ↔ un **insieme indipendente** di dimensione k in !G.

## 2
how that CLIQUE ∩ INDSET ≠ ∅. 

i only need to make the integer k equal to one elemet, that element became instantly both a clique and an indset. So for k = 1 we have CLIQUE ∩ INDSET ≠ ∅
