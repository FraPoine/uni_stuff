Let M -SUBSET SUM be a restriction of SUBSET SUM to only instances where all numbers x1, . . . , xn in the set (including the sum s) are not larger than M . 
The problem size can still be arbitrarily large, because the set can contain as many numbers as we want. 
Would 1000000-SUBSET SUM be still in NP? Would it be NP-complete? Would it fall back to P?


# guarda il 16