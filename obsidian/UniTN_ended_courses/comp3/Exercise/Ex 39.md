Let M be a one-tape TM on the alphabet Σ = { -, 0, 1}.
Assume that the input is a string s ∈ {0, 1}∗ (i.e., with no blanks in the middle) and that the machine assumes that the initial position is on the input’s leftmost symbol. 
1) Suppose that M is started on some unknown position (which may fall inside or outside the input string). Define some “preprocessing” states so that the machine finds the correct initial position before starting its computation. 
2) Suppose that the machine starts d cells to the left of the input; how many steps is your solution going to take before it finds the correct initial position? Just provide an asymprotic estimate, 
	- e.g. O(d), O(d2). . . 
- Observe that we don’t know if the initial position of the machine is to the left or to the right of the input string, so we need to perform some back and forth steps. An example initial configuration of our machine might be:
- . . .- 1 0 1 1 - - . . . - - \[-] - - . . .

---
dai dai dai
allora
ecco
su su
quindi

#### 1
- partire in un posto a caso
- devo trovare il posto giusto
- possibilità
	- prima di tutto controllo che il punto di partenza sia vuoto
	- il punto di partenza è vuoto
		- metto uno 0 nel punto di partena e inizio a cercare a destra
		- se non c'è niente metto uno 0 e entro in controlla destra
			- controlla destra funziona che che si sposta e se trova il vuoto torna a sinistra di 0
		- poi torno indietro fino a fino all'ultimo 0 o 1 e faccio controlla sinistra e così via fino a che non trovo qualcosa
		
- ma posso fare anche sposta 5 aggiungendo degli stati e mettendo dei marker e con il controlla posso verificare se sono i miei marker oppure la stringa vera 
- il controlla non basta
- penso rimarrò con la mia soluzione iniziale del cazzo :)


